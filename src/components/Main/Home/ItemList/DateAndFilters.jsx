// Shows the current date and the CTA links for filters
import CurrentDate from "./CurrentDate"

const DateAndFilters = () => {
    return (
        <section>
            <CurrentDate/>
            <a href="#"> Filtros</a>
            <hr/>
        </section>
    )
}

export default DateAndFilters