import ItemList from "../ItemListContainer/ItemList"

// contains the main list of elements on sale
const Delay = () => {
    return (
        setTimeout(() => ItemList, 3000)
    )
}

export default Delay